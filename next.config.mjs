/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  poweredByHeader: false,
  // Server-side rendering 활용 (Citation Moat용 SSR HTML)
};

export default nextConfig;
